{
  "name": "simple-assessment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install --include=dev",
    "build:all": "npm run install:all && npm --prefix frontend run build && cp -r frontend/dist backend/ && npm --prefix backend run seed",
    "start": "npm --prefix backend start"
  }
}
