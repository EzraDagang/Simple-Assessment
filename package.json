{
  "scripts": {
    "install": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build && cp -r frontend/dist backend/",
    "start": "npm --prefix backend start"
  }
}